apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: echo
  namespace: default
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: gcr.io/cff-loggregator/echo
